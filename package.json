{
  "name": "bootcamp2-chrome-ext-lui618",
  "version": "1.2.0",
  "scripts": {
    "build": "node scripts/build-extensions.mjs",
    "test:e2e": "playwright test --reporter=list, html",
    "test": "npm run build && npm run test:e2e"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.1",
    "archiver": "^7.0.1"
  }
}
